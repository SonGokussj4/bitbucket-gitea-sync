[project]
name = "bitbucket-gitea-sync"
version = "0.1.0"
description = "Migration and later syncing of batch bitbucket repos to gitea"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "loguru>=0.7.3",
    "niquests>=3.12.2",
    "python-dotenv>=1.0.1",
]
